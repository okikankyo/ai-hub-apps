# ローカルでテストする時に使う例。GitHub Actions では Secrets/Variables に設定する。
# 送信先 Webhook URL（Slack Incoming Webhook / Discord Webhook / ntfy トピックURL など）
WEBHOOK_URL=
# slack | discord | ntfy | raw
WEBHOOK_TYPE=slack
# 送信せず標準出力だけ見たい時は 1
DRY_RUN=1
